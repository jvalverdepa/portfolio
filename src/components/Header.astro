<header class="fixed mx-auto inset-x-0 top-0 flex py-4 z-10">
  <nav
    class="mx-auto px-2 flex gap-x-4 transition-colors rounded-full border border-black bg-neutral-200 dark:bg-black/50 backdrop-blur-2xl"
  >
    <a class="py-2 px-4" href="#">Home</a>
    <a class="py-2 px-4" href="#experience">Experience</a>
    <!-- <a class="py-2 px-4" href="#projects">Projects</a>
    <a class="py-2 px-4" href="#contact">Contact</a> -->
  </nav>
</header>

<!-- <script>
  window.addEventListener("scroll", () => {
    const header = document.querySelector("header nav");
    const scroll = window.scrollY;

    if (scroll > 40) {
      header.classList.add(
        "backdrop-blur-sm",
        "bg-white/30",
        "border-zinc-500"
      );
      // header.classList.remove("opacity-80");
    } else {
      header.classList.remove(
        "backdrop-blur-sm",
        "bg-white/30",
        "border-zinc-500"
      );
      // header.classList.add("opacity-80");
    }
  });
</script> -->
